// Single source of truth for the app version + build date.
// No build step — bump BY HAND on every deploy (minor = feature, patch = fix).
export const APP_VERSION = "0.5.0";
export const BUILD_DATE  = "2026-07-15";
